
/**
 * 債務詳細条件
 */
export interface IDetailDebtCondition {
  /** 支払先コード(開始) */
  payeeCodeS: string;
  /** 支払先コード(終了) */
  payeeCodeE: string;
  /** 支払先名称カナ(開始) */
  payeeNameKanaS: string;
  /** 支払先名称カナ(終了) */
  payeeNameKanaE: string;
}
